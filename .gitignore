# Virtual environments
swift-agent-env/
venv/
.venv/

# Python
*.pyc
*.pyo
__pycache__/
*.egg-info/

# Secrets / config
.env
.env.*
.claude/

# Evidence & case data (chain of custody — never commit)
cases
cases/
/mnt/
nfury_extracted/
*-extracted/

# Training datasets (download from Mordor/OTRF)
datasets/

# Binary / regeneratable caches
*.pkl
mordor_cache.json
reports/patterns.db
reports/brain_state.json
reports/brain_state.json.bak
reports/brain_state.json.corrupted
reports/forensic_brain_state.json

# Forensic training datasets (download via forensic_data_agent.py)
datasets/forensic/

# Corpus samples (SHA256s + HA metadata) — rebuilt by build_corpus.py
data/corpus/
# calibrated_weights.json is a build artifact; uncomment to version-control it
# data/calibrated_weights.json

# Case-specific investigation output (contains victim/suspect data)
reports/*-investigation.json
reports/*-report.html
reports/*-auditor-transcript.json
reports/*-custom-agent-report.json
reports/*-memory-triage-report.json
reports/*-iocs.json
reports/shield-campaign-iocs.json
reports/audit_log*.json
reports/audit_log*.jsonl
reports/chain_of_custody.json
reports/triage_*.json

# Forensic audit run logs (case-specific, append-only during investigation)
analysis/forensic_audit.log
custom-agent/analysis/forensic_audit.log
custom-agent/reports/
docs/reports/

# One-shot retroactive patch scripts
custom-agent/patch_transcript_cmds.py
custom-agent/patch_verdicts.py

# Case analysis outputs inside custom-agent
custom-agent/analysis/
custom-agent/reports/

# Docs build output
docs/reports/
mordor_cache.json
custom-agent/mordor_cache.json
